# Plantilla de archivo de variables de entorno para el frontend.
# INSTRUCCIONES:
#   1. Copiá este archivo como `.env` en la misma carpeta.
#   2. Completá los valores según tu entorno (dev o prod).
#   3. Nunca subas el archivo `.env` real al repositorio.
#      El `.gitignore` ya lo excluye.


# URL base del backend Spring Boot.
VITE_API_URL=http://localhost:8080
